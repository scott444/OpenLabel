namespace OpenLabel;

/// <summary>
/// The unwind direction is the orientation of the label as it unrolls
/// </summary>
public enum UnwindDirection
{
    BottomOff = 1,
    TopIff = 2,
    RightOff = 3,
    LeftOff = 4
}